{
  "name": "ai-ads-backend",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only workers/index.ts",
    "dev:worker": "ts-node-dev --respawn --transpile-only workers/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/workers/index.js"
  },
  "dependencies": {
    "ioredis": "^5.3.2",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "ts-node-dev": "^2.0.0",
    "@types/node": "^20.14.10"
  }
}
